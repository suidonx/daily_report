## 取り組んだ課題一覧
- JavaScript Primer
	- 第1章 基本文法
## わかったこと
### 第1章 基本文法
- JavaScriptとはECMAScriptという仕様で動作が定められている。
	- ECMAScript
		- 共通な動作
		- 言語の核となる仕様
	- JavaScript
		- ECMAScript + 実行環境の固有機能も含む
		- 例えば、Node.jsはUIを操作する機能は不要

- JavaScriptとはどういう言語か
	- 元々Netscape Navigatorというブラウザのための言語
	- 大部分がオブジェクトである
		- プロパティ: 値;の集合
	- オブジェクト同士の関連で成立している言語

- 文はセミコロンで区切られる
	- セミコロンがなくとも行末に自動で挿入される仕組みもあるが、暗黙的なものに頼ると意図せぬ挙動が発生することもあるので常に置くようにしよう

- JavaScript strict mode
	- 厳格な実行モード
	- 古く安全でない構文や機能が一部禁止される
		- レガシー構文の禁止
	- "use strict";
	- 開発者が安全にコードを書けるようにJavaScriptの落とし穴を一部塞いでくれる。
		- 常にstrict modeで実行できるコードを書くことが安全なコードにつながる

- 実行コンテキスト
	- ScriptとModuleがある
	- コードを書く場合、2つの実行コンテキストの違いを意識することはない
		- Script
			- 多くの実行環境でデフォルトの実行コンテキスト
		- Module
			- デフォルトがstrict mode
			- module機能を利用したい時

- ECMAScriptの後方互換性
	- 過去に書いたJavaScriptのコードが動かなくなる変更はほとんど入らない

- 変数と宣言
	- varはECMAScript2015で消えてconst, letが追加

- varはなぜ使われないか
	- 変数への再代入は、変数の値は最初に定義した値と同じである、という参照透過性と呼ばれるルールを壊し、バグを発生させやすい要因として知られている。
		- 意図せず既存の変数を書き換える可能性がある
	- 後方互換性の維持のため、残されている
- let
	- 初期値を指定しない変数はデフォルト値としてundefinedという値で初期化される
	- `var bookTitle`でbookTitleがundefinedという値が入る
- const
	- 後から再代入できない変数を定義する。
	- 定数ではないので、const objとしてオブジェクトを定義すると、プロパティの値は変更可能。
	- ループ処理などは変数への再代入が可能なletを使おう。

- REPL機能
	- ブラウザやNode.jsなど多くの実行環境には、コードを評価してその結果を表示するREPL（read–eval–print loop）と呼ばれる開発者向けの機能
	- コードを評価して結果を表示する機能
## 次やること
- JavaScript Primer
## 感じたこと
さっと読んでコードを書いて身につけていきたい

## 学習時間

| today | total |
| ----- | ----- |
| 3h    | 1053h |

## 取り組んだ課題一覧
- JavaScript
	- ガチで学びたい人のためのJavaScriptメカニズム
## わかったこと
- Object.keys(obj)でプロパティ名を取得
	- obj.keys()では取得しない
- for ... of
	- Symbol.iteratorを探してイテレータを作る
		このプロパティを定義すると、反復可能に。
	- イテレーターに対しnextメソッドを呼ぶ

- Proxyオブジェクト
	- 元のオブジェクトの代わりにプロパティの取得、設定、定義などの操作を再定義できるオブジェクト
	- >プロキシーオブジェクトは一般的に、プロパティアクセスのログを取ったり、入力の検証、書式化、サニタイズを行ったりするのに使用されます。
	- const pxy = new Proxy(target, handler1);
	- targetに元のオブジェクト、handler1にget, setメソッドなどを実装

- localStorageでデータを保存する
	- 保存処理は重たい処理、非同期にしたい
		- Promiseの静的メソッドresolve().thenを使って非同期処理にする
	
## 次やること
- JavaScript
	- 鬼滅API
## 感じたこと
- async, await難しい

## 学習時間

| today | total |
| ----- | ----- |
| 3h    | 1112h |

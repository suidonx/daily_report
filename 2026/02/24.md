## 取り組んだ課題一覧
- React
	- 【Reactアプリ開発】を見る
## わかったこと
- useState、useContextの復習
- firebase
	- コレクション→document→data
- FireStoreをEnterpriseモードでDB作成すると、Firebase SDKの互換性がなく、繋ぎ込みしているはずのDBに繋がらないエラー
- レンダリング中にstateを変更するとstate変更により再レンダリングされ、無限ループになる
 - 関数の副作用
	 - 関数の引数以外の入力、返り値以外の出力を副作用
	 - I/Oを伴う
	 - コーディング上の副作用
		 - 変数を読んだり書き込んだり
		 - オブジェクトに変更を加えるなど
- 再レンダリング
	- 同じコンポーネントが更新
- マウント
	- コンポーネント関数が初めて作られる瞬間
	- 別ページに行くと、アンマウントされる。
- レンダリング→仮想DOMで差分比較、DOM生成してUI作る→その他副作用の処理
- ブラケット記法を用いたプロパティ定義
	- 動的にkeyを設定できる
```
const key = "key-string";
// Computed Propertyで`key`の評価結果 "key-string" をプロパティ名に利用
const obj = {
    [key]: "value"
};
console.log(obj[key]); // => "value"
```

- npm i --save-dev
	- 開発時だけ利用するパッケージ
	- devDependenciesに入る
## 次やること
- React
	- Todo ListのPR修正
	- ReactでTrelloクローンを作る
## 感じたこと
- これまでの復習になった。


## 学習時間

| today | total |
| ----- | ----- |
| 6h    | 1178h |

## 取り組んだ課題一覧
- ポートフォリオ用にherokuデプロイ
- 面接準備
- 技術用語の復習

## わかったこと
- herokuデプロイする上でheroku CLIで行う作業に漏れがあり、つまづいた。
	- `heroku login`
	- `heroku create -a <heroku_app>`
		- もしくはherokuダッシュボード上でappを作成して、git remoteに`git.heroku.com<repo>`追加
	- `heroku config:set`などで環境変数にSECRET_KEYなど追加
		- config.settingsを見て何が足りないか確認
		- ダッシュボード上でも可能
	- djangoのデプロイをする上でpythonコマンドを使いたいのでpythonのbuild packを入れる
	- sassのコンパイルのため、sassのbuild packを入れる
	- cloudinaryのaddonを入れる
	- postgresのaddonを入れる
	- `heroku run python manage.py migrate`でマイグレーションする
	- `heroku run python manage.py createsuperuser`でadminを作る
	- `git push heroku main`などでpushしてデプロイする
		- ダッシュボード上でもできる
		- デプロイ時、静的ファイルのエラーが出る時があるので、エラーメッセージ見て対応

- 技術面接の一例
	- RESTとは？
	- HTTPステータスコード
	- トランザクションとは？
	- バリデーション
	- Cookieとは？
- HCのdocs見つつ、1問1答で切るように整理する
## 次やること
- Web技術
## 感じたこと
- herokuデプロイに手間取った
- 用語はざっと理解できているが、ぱっと答えられるように整理する必要がある

## 学習時間

| today | total |
| ----- | ----- |
| 4h    | 1338h |
